name: Validate project specs

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  validate-specs:
    uses: L3DigitalNet/project-standards/.github/workflows/validate-specs.yml@v5.7.0
    with:
      standards-ref: "v5"
      strict-lint: false # set true to also fail CI on lint warnings
